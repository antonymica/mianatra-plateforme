apiVersion: v1
kind: Service
metadata:
  name: mianatra-frontend
  labels:
    app.kubernetes.io/name: mianatra-frontend
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mianatra-frontend
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
